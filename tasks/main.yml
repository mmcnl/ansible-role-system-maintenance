---
- import_tasks: reset.yml
  when: maintenance_mode == 'on' or maintenance_mode == 'off'

- import_tasks: on.yml
  when: maintenance_mode == 'on'

- import_tasks: off.yml
  when: maintenance_mode == 'off'
